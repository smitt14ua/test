{% extends 'base.html.twig' %}

{% block title %}{{ news.title|e }}{% endblock %}

{% block body %}
    <article class="mt-3">
        <h1>{{ news.title|e }}</h1>
        <p>{{ news.text }}</p>
    </article>
    <hr>
{% endblock %}
